# IDE & OS
.DS_Store
*.idea
.vscode/
.cursor/
.cursorignore

# Python
__pycache__/
*.pyc
*.pyo

# Environnement local (docker, enterprise code, dumps, filestore, secrets)
.local/

# Paths legacy (résidus des anciens repos)
docker-compose*.yml
docker-compose*.yaml
Dockerfile*
odoo.conf
odoo-*.conf
filestore/
prod_backup_v16/
staging_backup*/
staging_dump.*
*.pgdump
*.sql.gz
*.zip
scripts/

# Data exports (temporaires, souvent volumineux)
*.csv
# Mais toujours committer les CSV essentiels des modules Odoo (ACL, data)
!**/security/*.csv
!**/data/*.csv
prod_cow_*.json
update_cow*.sql

# Fichiers résiduels à ne pas committer
/staging

# Documents client confidentiels (devis, audits internes)
DEVIS_*.md

# Runbooks ops internes (ne pas push chez le client)
docs/MIGRATION_PROD_RUNBOOK.md
